

/// Private class for storing the step area chart data point.
class StepAreaData {
  StepAreaData(this.x, this.high, this.low);
  final num x;
  // final num x;
  final double high;
  final double low;
}
